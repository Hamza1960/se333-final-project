[project]
name = "se333-mcp-server"
version = "1.0.0"
description = "MCP server for SE333 test automation agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "httpx",
    "fastmcp",
]
